import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  headerInfo: {
    flexDirection: 'row',
    marginBottom: 25,
  },
  image: {
    width: 150,
    height: 200,
    borderRadius: 8,
  },
});

export default style;
